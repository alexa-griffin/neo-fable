#pragma once

/* each of these headers are "header only libraries" that consist of small inline
 * functions and macros that make life easier 
 */

#define SIZEOF_ARR(arr) (sizeof(arr) / sizeof(arr[0]))

#include "color.hpp"
